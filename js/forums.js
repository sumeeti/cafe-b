function joinForum(forumName) {
    alert(`You have joined the public forum: ${forumName}`);
}

function joinPrivateForum(forumName) {
    alert(`You have joined the private forum: ${forumName} (Invited Only)`);
}
